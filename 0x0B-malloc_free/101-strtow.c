#include "main.h"
#include <stdlib.h>

/**
 * strtow - splits a string into words
 * @str: string to be split
 *
 * Return: double pointer to words (Success),
 * NULL (Failure)
 */
char **strtow(char *str)
{
	char **wrd;
	
	return ();
}
