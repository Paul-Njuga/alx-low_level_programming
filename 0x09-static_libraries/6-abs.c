#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @n: int to check
 * Return: Absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
